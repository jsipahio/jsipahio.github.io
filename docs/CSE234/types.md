# CSE234 - STL Data Types

The Standard Template Library (STL) provides implementations of most common data structures. 

## Iterators
Before discussing the STL data types, we need to cover iterators. Iterators allow 
## Array
The STL `array<T>` class is a template class that wraps a normal C array. It has the same constraint as a C array, in that the size of the array must be known at compile time. However, it provides protections against out-of-range access, provides iterators
## Vector
## List (and Forward List)
## Map (and Unordered Map)
## Set (and Unordered Set)
## Stack
## Queue and Deque