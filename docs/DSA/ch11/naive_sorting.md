# Naive Sorting Algorithms

Sorting a collection of data is a fundamental problem in computer science. 

## Bubble Sort

```C++
template <typename T>
void bubbleSort(T arr[], size_t size) {
    
}
```

## Selection Sort

```C++
template <typename T>
void selectionSort(T arr[], size_t size) {
    
}
```

## Insertion Sort

```C++
template <typename T>
void insertionSort(T arr[], size_t size) {
    
}
```
