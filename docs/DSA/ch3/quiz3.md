# Quiz 3

## Questions

1. True/False: A std::vector automatically resizes itself when it runs out of space.

2. Multiple Choice: Which stream should be used for printing error messages?
   - A. std::cin
   - B. std::cout
   - C. std::cerr
   - D. std::string

3. Short Answer: What character does the extraction operator `>>` treat as a separator between input values?

4. True/False: The `getline()` function reads input until it encounters a newline character.

5. Multiple Choice: Which operator is used to write data to an output stream?
   - A. `>>`
   - B. `<<`
   - C. `&&`
   - D. `==`

6. Short Answer: What keyword is used to allocate memory on the heap for a single object?

7. True/False: Memory allocated with `new` should be released with `delete[]` if it was created as a single object.

8. Multiple Choice: Which memory region stores local variables and the call stack?
   - A. Heap
   - B. Text area
   - C. Data area
   - D. Stack

9. Short Answer: What is a pointer in C++?

10. Multiple Choice: Which of the following is a common result of using a pointer that has not been initialized properly?
   - A. Compile-time warning only
   - B. Segmentation fault
   - C. Automatic vector resize
   - D. Infinite loop
