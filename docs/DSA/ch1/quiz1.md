# Quiz 1
Submit a PDF, Word Document, or Text Entry with the answers to these questions.

## Questions
1. Which `git` command is used to retrieve remote files? (1 point)
- a. push
- b. pull
- c. commit
- d. add

2. What is the difference between `git` and GitHub? (3 points)

3. The command `git commit` automatically uploads all changed files to the remote repository. (1 point)
- a. true
- b. false

4. Which is not a Makefile component? (1 point)
- a. Targets
- b. Dependencies
- c. Comments
- d. Repositories

5. Fill in the missing Makefile components. (3 points)
```make
main: main.o class.o
???

main.o: ??? 
    g++ -c main.cpp

???: class.cpp class.hpp
    g++ -c class.cpp
```

6. What name case is being used for this name: `exampleName`? (1 point)
- a. Camel Case
- b. Snake Case
- c. Pascal Case
- d. Screaming Snake Case

7. Poor identifier names improve code comprehension. (1 point)
- a. true
- b. false

8. Assume we have a function named `actionExecute()`. What naming standard(s) does this name violate? Suggest a better name. (2 points)

9. Assume we have a class named `run_tests`. What naming standard(s) does this name violate? Suggest a better name. (2 points)
